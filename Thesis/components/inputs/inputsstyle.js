import { StyleSheet } from "react-native"
import {outlineColor} from "react-native-web"

export const inputsStyle =StyleSheet.create({
    container:{
        backgroundColor:'white',
        width:'100%',
        borderColor:'white',
        borderWidth:1,
        borderRadius:5,
        paddingHorizontal:10,
        marginVertical:10,

    },
    inputText:{
        color:"#3367FF"
    }
})